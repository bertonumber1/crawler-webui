__pycache__/
*.py[cod]

# Local runtime state
crawler.db
*.db-wal
*.db-shm
server.log
*.log
.env

# A browser session. data/ is already ignored and that is where it normally
# lives, but a cookies.txt is a credential and must never be committable from
# anywhere in the tree by accident.
cookies.txt
*.cookies.txt
cookies*.txt

# Docker runtime state
data/
logs/
.env.docker
